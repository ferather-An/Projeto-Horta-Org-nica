document.addEventListener('DOMContentLoaded', function() {
    // Código relacionado ao rodapé
    // Atualizar o ano no rodapé
    document.querySelector('.footer-year').textContent = new Date().getFullYear();
});